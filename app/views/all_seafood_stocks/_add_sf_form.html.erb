<div class="ui center aligned grid">
  <form accept-charset="UTF-8" class="ui form nifty_form" action="/all_seafood_stocks" method="POST">
    <input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
    <input type="hidden" name="seafood[stall_id]" value="<%= @stall_owner.id%>">
    <div class="field">
      <button class="ui fluid button">
        Submit
      </button>
    </div>
    <div class="field">
      <button class="ui fluid button" id="addFish">
        <i class="add square icon"></i>
      </button>
    </div>
    <div id="addFishForm">
      <div class="two fields">
        <div class="field">
          <label>Fish Name</label>
          <div class="ui floating fluid dropdown labeled search icon button" >
            <i class="icon">fish</i>
            <input type="hidden" name="seafood[fish_id][]" value="">
            <span class="text">Select Fish</span>
            <div class="menu">
              <% @fishes.each do |fish| %>
                <div class="item" data-value="<%= fish.id %>"><%= fish.english + " // " + fish.malay + " // " + fish.chinese%></div>
              <% end %>
            </div>
          </div>
        </div>
        <div class="field">
          <div class="two fields">
            <div class="field">
              <label>Qty(kg)</label>
              <input type="number" name="seafood[volume_kg][]" placeholder="in kilos">
            </div>
            <div class="field">
              <label>Price $$/kg</label>
              <input type="number" name="seafood[price_dollarsPerKg][]" placeholder="in SGD">
            </div>
          </div>
        </div>
        <i class="remove_field remove icon"></i>
      </div>
    </div>
  </form>
</div>

<div id="fishFormTemplate" style="visibility: hidden">
  <div class="two fields">
    <div class="field">
      <label>Fish Name</label>
      <div class="ui floating fluid dropdown labeled search icon button" >
        <i class="icon">fish</i>
        <input type="hidden" name="seafood[fish_id][]" value="">
        <span class="text">Select Fish</span>
        <div class="menu">
          <% @fishes.each do |fish| %>
            <div class="item" data-value="<%= fish.id %>"><%= fish.english + " // " + fish.malay + " // " + fish.chinese%></div>
          <% end %>
        </div>
      </div>
    </div>
    <div class="field">
      <div class="two fields">
        <div class="field">
          <label>Qty(kg)</label>
          <input type="number" name="seafood[volume_kg][]" placeholder="in kilos">
        </div>
        <div class="field">
          <label>Price $$/kg</label>
          <input type="number" name="seafood[price_dollarsPerKg][]" placeholder="in SGD">
        </div>
      </div>
    </div>
    <i class="remove_field remove icon"></i>
  </div>
</div>
